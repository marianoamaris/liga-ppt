/**
 * Configuración del anotador.
 *
 * El PIN de acceso que había aquí se retiró: la zona de anotadores autentica
 * contra el backend desde hace tiempo, y un PIN en el bundle del cliente no
 * protegía nada.
 */

/** Jornadas de fase regular que tiene una edición. */
export const JORNADAS_TOTALES = 6;
